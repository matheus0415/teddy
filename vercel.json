{
  "version": 2,
  "builds": [
    {
      "src": "apps/entry-app/package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npm run build",
        "distDir": "dist"
      }
    }
  ]
}
